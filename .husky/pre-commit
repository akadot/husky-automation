#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "{" > ./src/config/to_commit.json

for file in $(git diff --cached --name-only | grep -E '\.(js|jsx)$')
do
  echo "file:$file," >> ./src/config/to_commit.json
done

echo "}" >> ./src/config/to_commit.json


npm update
